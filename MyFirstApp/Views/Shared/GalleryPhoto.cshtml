
<div class="mt-5 mb-5">
    <h3 class="text-center mb-3">@ViewBag.galleryTitle</h3>
    <div class="row">
        @foreach (var galleryPhoto in ViewBag.gallery)
        {
            <img src=@galleryPhoto class="col-3 mb-4 object-fit-cover" />
        }
    </div>
</div>
